---
"iconsmith": patch
---

Analog draws the rest of the 20-set toward house compile.

Pause, play, chevron, arrow, bookmark, share, airdrop, and airplane become
families (recipe tokens, no `ANALOG_KINS` rows). Check filled is a circular
badge; sun gets eight short ticks; cloud sits on a floor; bell drops the
terminal dot. `star`, `compass`, `quokka`, and `xyzzy` stay unknown.
