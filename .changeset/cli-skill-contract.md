---
"iconsmith": patch
---

Return structured CLI failures with stable codes and recovery hints, support piped SVG linting and rendering, expose a command schema, and align skill guidance with prepared requests and the bundled revision.
