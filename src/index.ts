/** Public API. CLI-only concerns stay in cli.ts so this can also back an MCP server. */
export type {
  Box,
  DotRole,
  DrawOp,
  Fingerprint,
  IconDoc,
  Issue,
  Keyline,
  Part,
  Provenance,
  Segment,
  Severity,
  Subpath,
} from "./types.js";
