# REACH implementation notes

Executor: append every deviation the code forced on `REACH.md`.

## Deviations

- Open Collider was motivation, not a spec. No condition A/B/C/D module, no academic-domain briefs, no “be original” line.
- Did not grow a six-row construction catalog (`bars-n`, `dots-n`, `ring-slash`, `body-nub`, `tiles`, `frame`). Analog collides the three constructions that already speak (`stack`, `trays`, `hub`). Volume of empty composers would still be a local max.
- Did not add `twinProgram` for part-based stacks. `trays` is primitives, so outlined analog does not need a fill-expand of `ellipse-flat` to ship `new`.
- Did not retarget `scripts/demo.ts` at reach. The product command is `iconsmith new`; the demo remains the mark language canary.
- Unmatched keyed subpaths become local house parts so compile works without extracting the variant. The test that expected CompileError on an empty vocabulary now expects a local part.
- `--look` is opt-in. Default analog uses a name hint rather than three vision calls per `new`.
