# Overviews

This directory is regenerated from selected attempts. It compares recurring objects and visual properties across the campaign: optical shapes, cuts and gaps, modifiers, hinting, filled strategies, dots and notches, diagonals, and cohorts.

The Central corpus remains the visual specification. Cursor's measurements are workflow inspiration, not design tokens for this set.
