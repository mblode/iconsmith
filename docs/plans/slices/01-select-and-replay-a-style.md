# 01 Prove a contrasting-style generation and evaluation loop

Read the [authoritative plan](../reference-guided-foundry.md) and [experiment protocol](../reference-guided-foundry.audit.md). This local ticket supersedes the earlier selector-first ticket; its path is retained for existing links.

**What to build:** A revision-selected engine/tournament run produces newly generated constrained programs, exact SVGs, native-size specimens and complete verdict/spend evidence for the house baseline and one contrasting collection. First test representational coverage, then generation and instrument sensitivity. A deterministic fixture proves wiring; a bounded real run supplies quality evidence. This is not a claim of professional equivalence.

**Blocked by:** a minimal authorized source fixture. Full collection inventory in 02 can proceed alongside. No Studio selector is required yet.

**Execution status, 5 September 2026:** revision plumbing, exact replay, native review wiring, cost retention and advancement checks are implemented. The offline compiler lab passes; the real pilot remains `blocked`. See [implementation notes](../reference-guided-foundry.notes.md) for commands, evidence and outstanding work.

## Acceptance criteria

- [ ] Existing house behavior remains pinned and replayable. Unknown or unavailable revision fails before paid work.
- [ ] Source/master/compiler identity survives generation, lookup, candidate extras, repair, twins, judging and replay; no hidden house fallback.
- [ ] Capability fixtures identify which target constructions the host can represent and their residual errors. Reconstruction is labeled separately from new generation.
- [ ] Clean generated-only programs are eligible without copied parts; irrelevant part stuffing and unapproved extras cannot establish acceptance.
- [ ] Audit rubric and native render sizes come from the selected revision/master. Prior house calibration is not reused for a new style.
- [ ] The evaluator is tested on same-concept defects and valid controls under the audit's frozen instrument protocol; unsupported sensitivity is reported.
- [ ] Direct constrained and visual-proposal routes use equal total budgets and the same inputs; every final artifact is compiled from a constrained program.
- [ ] Automatically generated family anchors are pinned and used for sibling comparisons.
- [ ] The 24-concept pilot separates development, sealed completion and sealed novel-family tasks. Failures remain in the denominator; exposed holdouts cannot be reused as fresh evidence.
- [ ] Attempts, first-pass acceptance, repair outcomes, costs including failures/unknowns and zero manual artwork intervention are recorded.
- [ ] The frozen advancement rule returns `pilot-proven` only if every declared pilot item/variant and required instrument class passes within budget with demonstrated new generation; otherwise `blocked`. Closing a blocked experiment cannot unlock 03.
- [ ] The experiment returns scoped evidence or a precise representation/generation/evaluator blocker. Its report does not label 24 successes as arbitrary-style qualification.

## Verification and finish

Run the plan's touched-scope tests, then the named gates appropriate to changed files. Record commands and artifacts in implementation notes. A real generation run requires an explicit total ceiling. Only the bounded experiment is ready for execution; no paid run was made while planning.


## Sharp-style capability finding, 5 September 2026

The compiler8 probe in `docs/log/sharp-style-capability-2026-09-05.json`
replays the admitted square/radius0 bookmark vertices at the same1.5 stroke.
It produces rounded corners because Canvas serialization fixes round joins;
the source uses the SVG default miter. At24px the pointed lower ends and top
corners visibly differ. This is a deterministic representation test, not new
AI authorship or a contrasting-style success. The revision schema separately
rejects radius0, and `line ... r0 solid` refuses before generation.

Next vertical slice: sharp Central outlined/filled bookmark capability, then a
fresh generated sibling pair. Extend the existing Spec and stroke-expansion
boundary with an explicit join policy, preserving round defaults. Admit a
zero-radius style coherently, including its empty positive-radius tiers; do
not silently substitute a positive radius. Give closed sharp line contours an
explicit closed representation and solid behavior using existing host stroke
expansion/interior union. Keep model vertices constrained and computed miter
points host-owned. Bound miters explicitly and match the SVG and expanded
paint settings. Do not merely change the SVG attribute: filled geometry and
replay must agree. Bump compiler revision and retain compiler8 artifacts.

Verify source reconstruction separately from novel generation. Required
fixtures: closed convex/concave sharp contour, closure join, open terminal,
miter-limit fallback, filled ring versus solid topology, exact program and
nested-recipe replay, and unchanged default round behavior. Inspect24px and
16px downsample against actual sharp source neighbors. Only then spend a
native author run on an unsupplied target. This addresses one contrasting
construction family, not arbitrary-style qualification.

Host foundation D054 now implements tested miter expansion in tools/stroke.ts. The source-coordinate paired proof uses explicit nonzero fill rules for ring/interior union; evenodd produced extra contours. Canvas, Spec, zero-radius admission and accurate miter painted bounds remain the next integration slice. Compiler stays8 because existing public drawing semantics are unchanged. See docs/log/miter-host-proof-2026-09-05.json.

A068 exposed square cap mismatch on external folder modifiers. Host tools/stroke.ts now supports verified square caps as an optional argument; default remains round. Wire a selected strokeCap policy through SVG, all filled line/arc expansion and actual painted bounds together. Closed contours must ignore cap choice, while trim creates new exposed ends. Do not limit the fix to vertical bars: diagonal square caps extend by tangential plus normal offsets. Keep historical compiler10 outputs and bump the compiler when drawing semantics become selectable. Host proof: docs/log/square-cap-host-proof-2026-09-05.json.


A069's small hollow play counter failed native-size confirmation. Direct source
inspection supplies the missing distinction: square-outlined play-circle has a
filled triangle and stroked circle, while standalone play is a larger stroked
triangle. Host proof mixed-play-proof-01 reuses the unchanged AI outlined folder
parent plus unchanged AI filled triangle, with clearer native silhouette.

Next slice: extend the existing explicit closed-line solid operation to an
outlined icon as a per-element filled modifier. Preserve its current expanded
stroke plus interior meaning, so this does not create another polygon API or
expose raw paths. Carry paint through the existing recipe, document,
transforms, SVG output and replay. Compute mixed painted bounds per element;
never expand the already-solid modifier again. Gap/angle/feature diagnostics
must know that modifier has zero stroke width. Refuse unsupported open or
missing-radius solid contours atomically. Preserve default whole-icon behavior
and old compiler artifacts; bump compiler for the new interpretation. Test a
real mixed play-circle-style composition, the A069 counter repair and nested
replay. Host derivation is evidence for this capability, not new AI authorship
or professional qualification.
