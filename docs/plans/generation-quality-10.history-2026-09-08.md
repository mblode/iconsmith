# Iconsmith: demonstrated Blode/Central quality

Authoritative implementation checklist. User-approved execution revision, 2026-09-07. Blode and Central are one house reference family. This execution order supersedes earlier sequencing below; historical receipts and failures remain preserved.

## User correction: AI-only evaluation

The user superseded all human-feedback dependencies on 2026-09-07: "No human feedback, use 100% ai". No human labels will be requested or fabricated. Replace human qualification with independent AI consensus, blinded house comparisons and objective host-injected defect controls. Report AI-assessed parity, never human-qualified parity. Existing historical pending human records remain preserved and cannot count as model validation. All thresholds below apply to AI assessments under this authority, with explicit reviewer identity and disagreement retained as uncertainty.

## Active execution phases

Three GPT-5.6 Sol agents own Runtime, Geometry and Evaluation; root integrates, freezes experiments, delivers images and records costs. Tasks close only with implementation and verification evidence; visual qualification remains separate.

### Phase 1 — reliable delivery and frozen experiments

- [x] Separate compiler correction from named visual repair under the original deadline-derived total retry ceiling. [V28 validation](../log/separate-repair-budgets-integration-v28-2026-09-08.json):1830tests/158files/zero skipped plus build/typecheck/check; exact compile-then-visual regression passes. Live improvement remains unproven.

- [x] Verify process-group ownership plus recursively observed descendant termination before validation. The teardown freezes observed topology, kills known descendants even after inspection failure, and refuses unproven settlement. Eight lifecycle regressions pass in the complete v9 run: 1,761 tests / 150 files / zero skipped. Earlier v5/v6 failures remain preserved; complete containment of already-reparented double forks is still open below.
- [x] Implement ownership-bound opt-in container lifecycle and pass a real detached-session writer timeout probe:510bytes written, container removed, no subsequent writes, timed-out artifacts ineligible. [Live receipt](../log/container-containment-probe-v4-2026-09-07.json). Native provider executables/authentication are not yet run in this Linux runtime.
- [ ] Prove containment of descendants that detach between process samples. The v10 adversarial double-fork test demonstrates the gap; real results explicitly report `process-group-and-observed-descendants`. Full macOS process-tree ownership remains unproven despite 1,762 passing tests / 150 files / zero skipped.
- [x] Implement guarded interrupted-candidate salvage with immutable inputs, successful host checks, exact image inspection and author review; retain interrupted provenance. Verified by focused lifecycle tests and prior controlled A120 recovery.
- [ ] Demonstrate interrupted salvage under the final frozen route and complete independent review within the original deadline.
- [x] Fix native self-review directory ownership with production-like exclusive-mkdir regressions; integrated v19 passes1795tests/155files/zero skipped. A136 retains four host-valid incomplete drafts; A137 recovery was not dispatched after its safe window elapsed. [Draft comparison](../log/a136-incomplete-draft-comparison-2026-09-07.png). No recovery or delivery success is inferred.
- [x] Separate finalization-only recovery and evidence clarification from geometric repair; allocate reserves once from the original deadline. Focused lifecycle regressions verify no-paperwork redraw and unchanged programs.
- [x] Bound construction/inspection/finalization separately and preserve the exact prior inspected candidate after failed repair construction or inspection. [Integrated v21 evidence](../log/runtime-review-integration-v21-2026-09-07.json):1820tests/157files/zero skipped; live recovery improvement remains under test.
- [x] Correct byte-identical attachment accounting while retaining ordered multiplicity and named provenance. [v22 verification](../log/runtime-attachment-integration-v22-2026-09-07.json):1821tests/157files/zero skipped; A143 original trace revalidated offline, delivery recovery remains separate.
- [x] Add immutable review-budget execution and exercise the actual callback path offline, including deadline/identity rejection and admission margin. [v24 validation](../log/review-budget-integration-v24-2026-09-08.json):1825tests/158files/zero skipped. A145 failed attempts remain visible; this is not reliability qualification.
- [x] Restore Astra native compatibility in an isolated prerelease CLI after stable-runtime provider rejection. [A153 receipt](../log/a153-alpha-compatibility-results-2026-09-08.json) verifies one exact-image call; this is compatibility only, and subsequent experiments require a fresh runtime freeze.
- [ ] Verify stage allocation on the frozen six-request replay; A126 folder16 currently exposes an independent-review timeout.
- [x] Pin executable realpath, bytes and version, actual Node/dependency runtime, tooling, specs, references and reviewer; revalidate before and after calls. Runtime tests and A125/A126 manifests provide evidence. The final selected route must be frozen again after integration.
- [x] Replay four frozen failures and two successful controls. A126 delivers all six pairs/12 slots within the deadline: three review-clear, two review-incomplete, one needs-repair. [Complete outcomes](../log/a126-reliability-results-2026-09-07.json). This completes the replay, not the reliability acceptance gate or a visual-uplift claim.

### Phase 2 — whole-library house construction

- [x] Wire catalog semantic roles and aliases into independently verified family packets; distinguish canonical family identity from coarse morphology. Catalog/retrieval regressions pass.
- [x] Expand explicit retrieval exclusions through catalog family membership, aliases and byte-identical copies; a regression covers differently named variants. Ordinary non-novel target exclusion retains useful neighboring donors.
- [x] Implement immutable metadata exposure receipts and fail-closed novel split validation for declared families, aliases, derivative clusters and input lineage; four tests pass. This is a metadata boundary, not proof of complete census or qualification.
- [ ] Complete close-derivative exposure validation for the unseen-family benchmark splits, including prior experiment exposure and caches.
- [x] Encode source contour, counter, terminal, junction, repetition and modifier guidance in v2 family packets. Packet hashes bind measured source structure and native-size guidance; packet tests pass.
- [x] Add explicit eight-class catalog routing with unknowns retained; [census](../log/house-construction-coverage-2026-09-07.json) has1587resolverrows/1243unclassified. Paired paints are folded; labels remain metadata-only, not visual coverage.
- [x] Add explicit host-derived centered part placement across the DSL/program API, including rotated asymmetric extents, grid quantization and malformed syntax regressions. Compiler21 passes1807tests/155files/zero skipped. [Exact before/after counterfactual](../log/a139-centered-placement-counterfactual-2026-09-07.json) preserves prior SVG/native bytes and repairs observed modifier placement; AI craft assessment remains separately gated.
- [x] Diagnose host filled-counter expansion across bell, keyframe and zoom contours. [Corrected prototype and native board](../log/filled-hole-clearance-prototype-v2-2026-09-07.json) preserve outer contours and remove observed fill-rule seams; bell16 clearance and production optical support remain unqualified.
- [ ] Demonstrate AI-assessed craft improvement across every construction morphology.
- [x] Validate connected/overlapping cutters, tangencies, nested holes, narrow bridges and rounded joins under actual SPEC16/SPEC24; six topology regressions and [light/dark boards](../log/house-geometry-boards-receipt-2026-09-07.json).
- [ ] Qualify full folder-lock native masters. Minimal endpoint correction restores the base; A126 native16 shackle remains an observed concern.
- [x] Implement native16/native24 guidance and artifact-bound cross-master review, allowing deliberate optical differences. Regression tests verify native dimensions, exact hashes, sibling completeness and modifier evidence.
- [ ] Validate actual generated cross-master family identity and native legibility under qualified AI review.
- [x] Keep paint-distinction metrics advisory and prevent automatic craft approval.
- [ ] Calibrate paint behavior against blinded AI assessments of house controls.
- [x] Re-run full source regression: 4,357 exact replays, 3,921 admitted, 436 refused, 1,007 separately tracked source raster differences, no regression. [Receipt](../log/house-source-regression-2026-09-07.json). Compiler21 repeats the full population with zero per-source admission or replay regression: [new receipt](../log/house-source-regression-compiler21-2026-09-07.json).
- [x] Account for every source difference and refusal by semantic family and construction class. [Full triage](../log/house-source-regression-family-triage-2026-09-07.json) and [interpretation](../log/house-source-regression-family-triage-v2-2026-09-07.json) reconcile 1,007 differences, 436 refusals and 388 overlapping files. Of 700 affected families, 95 have all sources refused and 605 retain an admitted source; affected does not mean generation-blocked.
- [ ] Remediate the prioritized source limitations and qualify the affected generated families; exact reconstruction is not visual parity.

### Phase 3 — matched author and refinement experiments

- [x] Demonstrate separate compiler and visual repair on the same bicycle16 development failure. [A157 review receipt](../log/a157-bicycle16-independent-review-results-v2-2026-09-08.json):six author/four reviewer calls settle; exact exposed house reconstruction, craft9/8 and both ship unchanged. Median8.5 remains below9; critic and whole-library gates remain open. [Actual output](../log/a157-bicycle16-attempt-board-2026-09-08.png). Frozen launch descriptor defect is preserved with explicit actual-dispatch evidence.

- [ ] Compare shared packets against independent retrieval on six families, both paints and sizes.
- [ ] Screen available native Astra/Claude authors on four difficult concepts; extend surviving contenders to 20 families/80 outputs.
- [x] Diagnose first matched shield-check native16 drafts with two AI reviewers. [A138](../log/a138-draft-diagnostic-results-2026-09-07.json): Astra craft3/2 with critical placement collision; Claude9/8, both ship, median8.5 below9target. Fourreviewcalls complete; incomplete author delivery and one exposed concept prevent winner/qualification.
- [x] Prepare four broader development families at both masters with explicit reference exposure and32source admissions: jellyfish, bicycle, scissors, credit-card-check. [Packet receipt](../log/next-morphology-packets-v2-2026-09-07.json). These are admitted inputs, not completed outputs or visual qualification.
- [x] Complete bell-pause native16 lifecycle screen and independent AI review. [A141 results](../log/a141-independent-review-results-2026-09-07.json):2of4slots delivered; Claude repair timed out; delivered Astra pair scores9/6 with family/native/ship disagreement. [Actual board](../log/a141-author-comparison-board-2026-09-07.png). Ten nativecalls total, no route winner or qualification.
- [x] Test matched native reference presentation without redrawing the candidate. [A142 results](../log/a142-native-anchor-review-v2-results-2026-09-07.json): both reviewers approve family/native/ship; craft9/7 has median8 and fails the target. Four calls, development evidence only.
- [x] Diagnose scissors native16 retained outputs and separately review recovered Astra. [A146](../log/a146-recovered-astra-review-results-2026-09-08.json): two reviewers recognize and ship, craft9/7 median8 fails target; original deadline review absent and Claude delivery incomplete. [Actual outputs](../log/a143-scissors-retained-output-board-2026-09-07.png). No route qualification.
- [x] Complete jellyfish native16 matched author screen and independent review within the original deadline. [A147 results](../log/a147-jellyfish-independent-review-results-2026-09-08.json): Astra delivered2slots, Claude construction timed out;8nativecalls total. Craft9/6 and ship disagreement remain uncertain; the lower review invents a missing rim and fourth tentacle contradicted by exact pixels/DSL. [Actual board](../log/a147-jellyfish-review-board-2026-09-08.png). No repair or route qualification inferred.
- [x] Run a separately frozen medium-effort Claude author diagnostic on jellyfish16. [A151 terminal](../log/a151-medium-author-terminal-receipt-2026-09-08.json): four calls complete, zero delivered arms, rejection preserved; [actual failed images](../log/a151-medium-author-rejected-board-2026-09-08.png). Construction completion improves over the historical high timeout, but craft and delivery do not qualify.
- [ ] Compare targeted visual repair and bounded host parameter search using identical inputs and total budgets.
- [x] Measure one blinded placement-only host counterfactual. [A140](../log/a140-centered-review-results-2026-09-07.json): before craft3/2→centered9/7; both reviewers remove critical flag, but median8failscrafttarget and one reviewer retains tip-clearance concerns. This is exposed development evidence, not one of20sealedrepaircases.
- [x] Enforce finite parameter-search budgets, integer candidate limits, unique declared domains and deadline checks before invalid candidates; nine focused tests pass, with explicit considered/failed counts and cooperative cancellation scope. This validates search bounds, not visual improvement.
- [x] Run a bounded native16 semantic-parameter diagnostic with pinned parts/spec: 27 valid candidates, then enforce unchanged modifier center and counter topology. Independent blind Sol review rates both original and constrained candidate 8/10 with a tie; no craft uplift or route selection claimed. [Receipt](../log/folder-lock-native16-sol-comparison-2026-09-07.json).
- [ ] Run raster/vectorization feasibility only with an available budgeted adapter; require editable DSL for production eligibility.
- [ ] Freeze selected route before qualification; retain faster ten-minute route if quality matches the twenty-minute ceiling.

### Phase 4 — independent AI review and repair

- [x] Implement and verify recognition-before-meaning review, immutable artifact binding, independent reviewer adapters, uncertainty quarantine and objective-control scoring. The A130 source freeze passes 1,754 tests / 149 files / zero skipped, build, typecheck and lint. Small A125 calibration remains diagnostic; a 100-family sealed control population exists but has not been dispatched.
- [x] Add a frozen, resumable AI review CLI with explicit executable identity, before/after runtime and tooling checks, full artifact-tree validation and duplicate-dispatch protection. The actual A130 resume preserves both terminal hashes and makes zero new provider calls. Review delivery and critic qualification remain separate gates.
- [x] Implement separate critic-versus-AI-panel agreement gates, requiring sealed predictions, distinct identities, complete controls, at least 100 canonical stimuli, 20 confirmed critical cases and 20 predicted approvals. Missing, uncertain and conflicting panel labels prevent qualification. Tests establish the scoring behavior; no actual panel is qualified yet.
- [x] Convert active benchmark exports to AI-only pending records with two required reviewers, exact artifact hashes and empty review arrays. New exports create `ai-review.json`; historical human records remain immutable and supply no approval.
- [x] Add read-only assessment of actual AI campaign receipts with exact candidate/anchor hashes, stage/result cross-validation and two distinct provider/model identities. Critical findings quarantine; missing and uncertain evidence cannot approve. [A130 assessment](../log/a130-independent-ai-assessment-2026-09-07.json) retains all four outputs as ineligible and preserves the older Claude diagnostic scores without inventing provider provenance.
- [x] Deliver a complete native Astra two-stage review on four actual generated outputs with exact initial attachments and no tools. A131 completes two calls in76,495ms, recognizes4/4 and rates all four9/10. [Bound receipt](../log/a131-attachment-review-results-2026-09-07.json). Claude's earlier7/6/3/5 ratings remain visible; this disagreement and the small population prevent qualification.
- [x] Add a latched campaign stop sentinel checked before each provider boundary, with stopped/unstarted states and idempotent resume; ten campaign tests pass. It prevents later dispatch, while in-flight cancellation and full containment remain separate.
- [x] Correct mixed-size packet composition and verify all six input raster regions remain exact and unscaled; inspect all ten corrected control sheets. Preserve cancelled A132 as invalid evidence.
- [x] Require exact-artifact semantic-label provenance from two independent AI identities before recognition can qualify. Filename-only, missing, conflicting or mismatched evidence stays diagnostic; 23 focused qualification tests pass. Actual semantic population validation remains outstanding.
- [x] Complete corrected ten-control development review with both pinned native reviewers and validate terminal evidence. [A133 diagnostic](../log/a133-ai-control-development-diagnostic-2026-09-07.json): each detects5/5injected defects; frozen-key recognition tallies9/10Astra and7/10Claude, with S005 label/choice validity under audit (both identify a power symbol absent from esc-based choices). These are not validated recognition accuracy. One contradictory Claude critical/ship answer stays quarantined. Ten exposed exact-target controls do not qualify the critic.
- [x] Prepare delivered-only review boards from exact terminal/inspection-bound proof panels, matched native anchors and immutable review evidence. Nine helper tests and an actual A141 offline preparation pass; included in [v21 validation](../log/runtime-review-integration-v21-2026-09-07.json).
- [ ] Collect independent AI assessments in blind packets of at most20 outputs: recognition before names, craft, family fit, native legibility, critical defects, ship unchanged.
- [x] Census the complete catalog against preserved exposure records:927prospective unique families. [Bounded-history preparation](../log/fresh-critic-complete-catalog-preparation-2026-09-07.json). Exact stimuli and semantic validation remain pending; this is not global unseen provenance.
- [x] Verify a third native exact-attachment model route, `gpt-5.6-sol`, on already exposed development evidence. [Live receipt](../log/sol-exact-attachment-feasibility-2026-09-08.json); one call, exact model and image bytes verified, instrument unqualified.
- [x] Exercise the first three-stage critic pilot with declared stop gates. [A149](../log/a149-critic-p01-offline-assessment-v2-2026-09-08.json): Sol complete, Claude craft timeout, Astra unstarted;6nativecalls and10retained stimuli, all independent panels uncertain. Development evidence only.
- [x] Support exact-byte repeated presentations with distinct stimulus IDs while retaining canonical uniqueness and excluding repeats from every sample denominator. [V26](../log/reviewer-controls-integration-v26-2026-09-08.json) passes1827tests/158files/zero skipped and integration checks. Identical/reversed control predictions remain outstanding.
- [x] Test medium-effort Claude on the exact exposed P01 critic packet. [A150](../log/a150-critic-medium-results-v2-2026-09-08.json): all three stages complete, three craft/ship contradictions remain uncertain; no quality-equivalence claim.
- [x] Attempt the missing Astra P01 diagnostic with fresh-output and native-receipt guards. [A152 accounting](../log/a151-a152-root-accounting-2026-09-08.json): provider rejects the pinned CLI as too old at stage one, one call, quiescent, no semantic-key disclosure or automatic retry. Runtime compatibility remains unresolved.
- [x] Complete P01 Astra under the compatible isolated runtime and assess all three historical profiles with exact evidence binding. [Cross-profile result](../log/a154-p01-cross-profile-development-assessment-2026-09-08.json): all routes complete; other-two-model panels remain largely uncertain. This is ten exposed development stimuli, not matched or sealed qualification.
- [x] Complete the isolated A155 inline Claude transport diagnostic. [Result](../log/a155-inline-claude-results-2026-09-08.json): one bound high-effort craft-stage call completed across ten exposed development images; it remains excluded from the three-stage critic panel and closes no phase gate.
- [x] Attempt the isolated A156 Astra bicycle16 author screen. [Terminal receipt](../log/a156-bicycle16-attempt-results-v3-2026-09-08.json) retains both paint proofs and four native process calls; the author rejected the pair for visible scale and counter defects, so independent review was inadmissible and no phase gate closed. [Evidence board](../log/a156-bicycle16-attempt-board-v3-2026-09-08.png).
- [ ] Qualify critic on100 independent sealed stimuli including20 critical defects; identical/reversed controls are additional.
- [x] Replay and render the retained hammer-check handle-only candidate against its previous best, with exact programs and native before/after evidence. [Board](../log/hammer-check-attempt3-comparison-2026-09-07.png), [receipt](../log/hammer-check-attempt3-offline-2026-09-07.json). Generic pair checks pass. [Independent gap measurement](../log/hammer-check-gap-measurement-2026-09-07.json) confirms clearance increases from1.2517 to1.6053 design units, crossing the1.5 minimum. [Independent AI diagnostic](../log/a134-hammer-ai-repair-diagnostic-2026-09-07.json) disagrees: Astra7→9, Claude7→6/uncertain; check geometry is unchanged. Attachment/spacing craft remains unresolved and the candidate is not promoted.
- [ ] Measure repair on20 separate independently classified repairable failures; require16 successes without critical regression.
- [ ] Keep tuning, development, critic test, repair test and novel-family populations separate; failed qualification requires a fresh sealed test. A132 exposed the ten P01/v3 controls for development; their rerenders/reviews cannot count as fresh independent qualification stimuli.

### Phase 5 — catalog and novel qualification

- [ ] Forecast resources from frozen measured cohort before campaign dispatch.
- [ ] Execute4442 catalog pair requests/8884 output slots with all failure states retained and a qualified critic on every output.
- [x] Freeze prospective catalog audit membership before dispatch: 100 semantic families / 400 slots, deterministic seed and catalog metadata hash, spanning all six current coarse strata. [Selection receipt](../log/catalog-ai-audit-preselection-2026-09-07.json). This is sample preparation only.
- [ ] Complete construction-class coverage annotation for the selected catalog sample, obtain blinded AI assessments and additionally audit every flagged/uncertain result. Coarse name-derived strata alone do not prove the required morphology coverage.
- [ ] Blindly compare generated outputs to house counterparts without revealing targets to generation.
- [ ] Execute two disjoint20-family novel batches/80 outputs each, excluding whole families and close derivatives from all generation inputs.
- [ ] Pass each rubric dimension and each paint/native-size stratum; report family-cluster uncertainty and sampled AI evidence honestly.

## Accepted defaults and completion gates

End-to-end ceiling1200000ms per concept-size pair, including both paints and all stages; finish early whenever possible. API cap remains50USD, earlier13.06220785 actual and11.37126575 reserved/unknown; no automatic API fallback. Native subscription dollars remain unknown. At the ceiling, the catalog is1481 serial hours, an upper bound rather than forecast. No public web UI, backward compatibility or publishing. Keep corpus in place. Fine-tuning is deferred until a demonstrated plateau, at least500 licensed corrected family-separated training examples, verified trainability and a separate budgeted held-out experiment.

Required: recognition>=95%, ship unchanged>=95%, median AI craft>=9/10, median paired house craft delta>=-0.5/10, family fit>=95%, native legibility>=95% per paint/size stratum; no accepted unresolved critical defect. All slots accounted for, >=95% successful completion within frozen deadline, exact replay and safe resume. Critic agreement with a separately sealed independent AI adjudication panel must achieve approval precision>=95%, critical recall>=90% and decision coverage>=80%, with consistent controls. Host-corruption recall alone cannot establish generated-craft approval precision; repair>=80%. Both novel batches pass independently. No AI-assessed10/10 claim until all gates pass.

## Earlier execution evidence and detailed implementation context

## Execution checklist

Historical checklist retained to explain earlier work. The active phases above supersede this section, including all earlier human-feedback dependencies, deadlines and sequencing. Prior experiments remain immutable in the foundry log.

- [x] 1. Bind audits to exact artifacts, compiler/parts/master and the complete population; enforce one end-to-end deadline.
- [x] Preserve source fill rules, mixed outlined ink and open curves; repair compound trim and retain Boolean recipes.
- [x] Audit all 4,357 sources under compiler20 with per-file dispositions and no admission regressions.
- [x] Export blind image packets and pending, image-hash-bound human labels.
- [x] Implement guarded interrupted-finalization recovery and verify offline regressions.
- [ ] 2. Finish and inspect the native16 shoulder repair; verify recovery through a real interrupted author run.
- [x] 2a. Introduce shared family reference selection with separate admission for each native master.
- [ ] 2a. Verify family consistency on multiple morphologies, both paints and both sizes.
- [ ] 3. Collect sealed human labels and qualify the critic on at least 100 independent stimuli.
- [ ] 3. Measure repair success on at least 20 separate human-classified failures.
- [x] 4. Prioritize unresolved source classes that block useful generation; retain explicit refusals. D121/D126/D127/D128 separate grid sensitivity, source semantics and the verified SVG line importer bug.
- [x] Repair digit-bearing SVG line attributes and verify all 4,357 sources: two additional admissions, zero regressions.
- [x] Implement bounded development execution with complete denominators, immutable inputs and identity-validated repeated resume.
- [ ] 4. Deliver all 80 outputs of the 20-family development cohort, with failures included.
- [ ] 5. Run a matched model screen and extend a qualifying contender to the development cohort.
- [ ] 5. Freeze the selected route and prepare measured campaign resource/batch manifests.
- [ ] 6. Complete all 8,884 catalog output slots and two disjoint 80-output novel-family batches.
- [ ] 6. Pass every declared visual, reliability, repair and efficiency gate before claiming qualified 10/10.

Earlier implementation verification: 1,656 engine tests, 132 files, zero skipped. Root build/typecheck/check pass. Historical logs: `.staging/sol-execution-2026-09-07/root-test-v6.log`, `root-types-v4.log`, `root-build-v3.log` and `root-check-v3.log` in that directory; [durable checkpoint](../log/sol-execution-checkpoint-2026-09-07.json). The A130 source freeze passes 1,754 tests / 149 files / zero skipped with root build/typecheck/check in `.staging/house-quality-execution-2026-09-07/root-{test,types,build,check}-v4.log`. The current A131 source freeze passes 1,761 tests / 150 files / zero skipped plus root build/typecheck/check; see [AI-only checkpoint](../log/ai-only-execution-checkpoint-2026-09-07.json) and v9 logs. These establish implementation behavior, not visual qualification.

## Outcome and definition of done

A requested concept produces a recognizable, well-drawn outlined/filled pair at the requested native size, using automatically retrieved references, constrained editable geometry, honest review and a bounded end-to-end runtime. The user can audit the actual images and reproduce their exact SVGs.

Scope is the whole Blode library and new concepts, not one cloud or a handful of attractive examples. The current inventory is 4,357 source SVGs and 2,221 filename concepts. Both paints at native16/native24 mean **8,884 output slots, or 4,442 concept-size pair requests**. Source inventory, source reconstruction and genuinely generated output are separate populations. Filename concepts are not yet verified semantic families.

“10/10” means every release gate below passes on its declared population, every accepted output meets the safety/craft contract, and remaining failures are explicitly rejected. It is not a promise to draw every imaginable concept perfectly. A small successful cohort cannot qualify the whole library. Do not average away a failed dimension or replace a missing human label with a model judgment.

No public web UI, backward-compatibility implementation, npm publishing, or training from scratch. Historical artifacts and their compiler identities remain immutable. The local CLI and standalone PNG/SVG/JSON audit files are the delivery surface.

## Verified baseline and open gaps

The previous **6.5/10** assessment is a developmental judgment, not a fresh measured score. No human-qualified score exists for the merged implementation.

| Current evidence | What it establishes | Remaining gap |
| --- | --- | --- |
| [Compiler20 admission](../log/library-parts-admission-compiler20-2026-09-07.json) | 3,919 of 4,357 source files admitted, 8,144 components, 438 refused; no admission regressions | Native fidelity and unsupported paint/semantics still exclude donors |
| [Compiler20 reconstruction](../log/library-replay-compiler20-2026-09-07.json) | 4,357 exact replays, zero replay failures, 1,009 source pixel differences | Exact replay is not source fidelity or generated quality; conversion causes remain partly unresolved |
| [Per-file dispositions](../log/library-failure-dispositions-compiler20-2026-09-07.json) | Refusals and source differences retained separately with overlapping membership | Prioritize failures that actually block composition; do not add overlapping counts |
| [Compiler20 repair audit](../log/native-repair20-2026-09-07/batch-001.png) | Bicycle16, bell-pause16 and folder-lock24 delivered review-clear in 383,493 / 360,574 / 492,260 ms; folder16 incomplete | Four-request development evidence; reviewer unqualified, human labels pending |
| [Shared-reference audit](../log/native-family-repair-2026-09-07/batch-001.png) | Shared folder donor restores continuous body; additional folder16 run takes 526,934 ms and remains needs-repair | Cutout shoulder unresolved; second attempt interrupted; this mixed-route audit is not a matched comparison |
| [Counter rendering probe](../log/render-counter-probe-2026-09-07.txt) | Stored and fresh PNGs identical; sampled counter remains white at all tested sizes | Display observation uncertain; no reproduced host-render bug justifies a renderer rewrite |
| Existing audit, deadline and recovery tests | Pinned replay, missing-row accounting, bounded recovery and refusal of tampered/uninspected artifacts | Controlled A120 validates real recovery; its retained visual uncertainty remains unresolved. Shared retrieval is implemented; human family-fit acceptance remains pending |
| `src/eval/foundry-gate.ts` | Explicit precision, recall, coverage and control gates | No sealed human-labeled critic qualification or independent repair-rate measurement |

The original [12-image pilot](../log/native-repair-pilot-2026-09-07/batch-001.png) remains historical: two of six requests review-clear within budget, four exhausted the deadline. Do not pool it with compiler20 retries to imply a frozen-route success rate. Mountain-bike retrieval for bicycle is useful development evidence, not unseen-family generalization.

## Rubric and acceptance populations

Freeze these thresholds and the population before generating results. Report numerator, denominator, missing/failed/uncertain counts and uncertainty intervals. For novel-family statistics, report intervals clustered by family; two small batches do not establish population-wide certainty.

| Dimension | Current judgment | Release gate |
| --- | --- | --- |
| Recognition | 7/10 | ≥95% correct blind recognition in every paint/native-size stratum, with predefined synonyms and distractor/unknown controls. Zero accepted outputs convey a conflicting action. |
| Visual craft | 6/10 | ≥95% of requested outputs approved without manual geometry edits; human median craft≥9/10. Every accepted output approved, with zero unresolved critical slivers, broken tangencies, accidental seams or collapsed counters. |
| Reference/style matching | 7/10 | ≥95% blind family-fit pass in each stratum. No unexplained cap, radius, weight or modifier-polarity violations in accepted outputs. Review both paints together as well as individually. |
| Small-size legibility | 5/10 | ≥95% native16 and native24 pass on light/dark backgrounds. Accepted counters, thin connections and modifiers remain distinct. A24construction rasterized at16 cannot count as a calibrated16master. |
| Geometry/SVG reliability | 9/10 | Every accepted SVG parses, conforms and exactly replays with its pinned compiler/spec/parts. Every reusable component passes source admission. Invalid operations fail atomically; no fake background holes, recipe loss or unsupported semantics silently accepted. |
| Autonomous evaluation/repair | 5/10 | On separate sealed human-labeled controls: approval precision≥95%, critical-defect recall≥90%, decision coverage≥80%; identical/reversed controls fully consistent. Resolve≥80% of human-classified repairable failures within budget on a distinct repair set, with no critical regression. |
| Efficiency/predictability | 6/10 | ≥95% of requests complete within the frozen end-to-end budget; provisional p95≤10minutes per concept-size pair, including retrieval, authoring, review and repairs. All terminal states and usage receipts complete. Report cost per accepted pair, including failed requests. |

Apply the visual/recognition/efficiency thresholds to the completed catalog campaign and independently to each novel qualification batch. All8,884catalog slots must have explicit terminal evidence; missing rows prevent qualification and refusals count against yield. Low error among accepted rows alone cannot hide a pipeline that rejects most requests. Geometry regression covers all4,357source files, including explicitly unsupported conversions; it does not require falsely admitting every source format.

## Execution order and dependencies

Use the existing canonical harness. Do not add another generation implementation. Paths below are relative to `packages/iconsmith/` unless marked otherwise. Each slice delivers code, named regression evidence, actual images where relevant, and a foundry-log receipt.

### 1. Preserve the completed evidence and deadline boundary

Completed in the merged baseline. `scripts/quality-benchmark.ts`, `scripts/quality-audit.ts`, `scripts/local-generate.ts`, `scripts/local-retrieval.ts` and `scripts/local-style-run.ts` already bind rows to original SVG/program hashes, revision/spec/parts and native master. One absolute 600,000 ms deadline includes auth, retrieval, authoring, independent review and repair. Terminal status distinguishes construction failure, incomplete delivery, deadline exhaustion and pending human review.

Keep regression coverage for wrong master, missing dependencies/rows, tampering, expired retrieval budgets and late completion. A24 artifact does not fill a native16 slot. Preserve original images; replay is a comparison. Required-library automatic retrieval and pinned-control routes remain explicitly distinguished.

### 2. Close a real repair and finalization loop

**First vertical slice:** finish the retained folder-lock16 cutout shoulder using the existing DSL, then deliver a complete inspected result through the canonical harness. This isolates a known defect before generalizing the mechanism across families.

Affected boundaries: `scripts/local-style-run.ts`, its tests, `scripts/local-review.ts`, `src/tools/boolean-round.test.ts` and existing Boolean implementation only if a minimal fixture proves a missing capability.

- [ ] Reproduce the circle/rectangle cutter join in A119 attempt1. Complete and inspect the retained `union r1` alternative in a fresh revision/run; it is an unverified candidate, not an accepted fix. Preserve counter width, native clearance and donor contour quality.
- [x] Verify real interrupted finalization can resume a checked, inspected, unchanged program within the original deadline and three-attempt cap. Use a controlled author interruption if a natural interruption does not occur; label the experiment accordingly. Require the model's own final inspection and review receipt.
- [x] Confirm invalid inputs, absent programs, missing image inspection, exhausted budget and repeated interruption remain terminal refusals. Never manufacture a review receipt or promote an incomplete attempt.
- [x] Export the complete outlined/filled pair with native16 light/dark and enlarged proofs, exact replay and the preserved failed attempts. Request actual human optical labels through the existing packet.

**Acceptance:** the shoulder has no visible kink or collapsed counter in independently inspected native/enlarged evidence; no unresolved critical author or reviewer finding can be overridden by another pass. Real-run recovery evidence proves artifact integrity, unchanged deadline and honest review. Human acceptance remains a separate pending gate until labeled. Do not rewrite rendering based on the unreproduced PNG observation.

### 2a. Make reference choice consistent across the family

Can begin alongside slice2 using existing fixtures. Needed before the frozen 80-output cohort and any catalog campaign. A119 suggests shared donor topology helps, but changing several inputs together did not establish a winning strategy.

Extend `scripts/local-retrieval.ts`, `scripts/family-parts.ts`, `scripts/reference-proofs.ts` and the canonical `scripts/local-generate.ts` boundary. Reuse `createStyleRevision`, `selectStyle` and `admitFamilyParts`; do not create a second generation pipeline.

- [x] Select a family-level source packet once, with stable hashes, source identities and explicit body/modifier/polarity intent across both paints. Persist selection evidence and exclusions for reproducible reuse.
- [x] Independently admit the selected sources under each target master's spec and export native16/native24 proofs. Rebinding a24 packet's master identifier is insufficient. A donor that fails target admission stays reference-only/refused; an alternative requires a recorded family-consistency decision.
- [ ] Preserve native optical freedom: weight, gap and detail suppression may differ while body topology, modifier position, meaning and family identity remain coherent. Do not require identical outlined/filled silhouettes or flatten curves to force agreement.
- [x] Add regression scenarios for mismatched packet/source hashes, missing master, failed target admission, excluded-family leakage and unintended donor switching. Cache reuse requires exact selection/spec/source identity.
- [ ] Compare current independent retrieval against shared packets on six predeclared development families: folder-lock, bell-pause, bicycle, cloud-upload, shield-check and an asymmetric tool/modifier family selected before the run. Hold author, reviewer, specs and budgets fixed; both paints at16/24 means 24 outputs per arm. Preserve all failures and compare family fit, native legibility, terminal yield and total time.

**Acceptance:** paired-size proofs establish coherent families across multiple morphologies without native-size quality regression. Blind human family-fit labels are required before choosing the default; without labels the experiment remains development evidence. Retrieval time is charged to actual end-to-end requests: report cold selection and reused-packet timing separately, never present warm-cache results as cold latency. If shared selection harms a family, retain the failure and test an explicit exception within the same route rather than silently switching donors.

### 3. Build the human label and qualified repair path

Evidence export is implemented. Label collection can proceed while slices2 and2a engineering continue. Qualification depends on actual human responses.

Extend `scripts/quality-benchmark.ts`, `scripts/local-review.ts`, `scripts/review-construction.ts`, `src/eval/foundry-gate.ts` and `scripts/local-style-run.ts`.

- Export local blind audit batches of at most20images with separate answer keys and editable label JSON. Collect recognition before revealing concepts, then craft rating, family fit, native legibility, critical defects and “ship unchanged”. Human identity and image hashes bind labels to evidence; null is pending.
- Gather at least100distinct canonical labeled stimuli for the sealed critic test, separate from critic tuning, development output and novel qualification families. Include≥20critical defects and enough human-approved examples to exercise the existing≥20predicted-approval gate. Do not count presentation duplicates as independent stimuli.
- Keep identical-image and reversed-order presentations as controls, outside that100-stimulus count. Freeze prompts/thresholds before revealing the critic test labels. A failed test becomes development data; reserve a fresh test for requalification.
- Benchmark repairs on a separate set of at least20human-classified repairable failures across multiple morphology families. Each repair identifies one observed defect, preserves the prior best candidate and rechecks unchanged requirements. No progress, repeated defect, representation gap or deadline ends the run explicitly.
- Only a passing, versioned critic qualification can enable automatic craft approval for that exact instrument configuration. Until then deliver review-clear as pending human audit, never approved. Any instrument/model/prompt change invalidates qualification until retested.

**Acceptance:** Existing `qualifyCraftJudge` gates pass on real labels, not fixtures. Wrong hashes, missing labels, duplicate stimuli, always-approve/always-reject critics and inconsistent controls fail. Repair success is≥16/20 for a20case set, without critical regressions; keep failures in the denominator. Human labels are an external dependency, not a reason to stop independent geometry work.

### 4. Expand from the repair cohort to all library failure classes

Source work can proceed now. Freeze the family strategy before collecting the comparable 80-output cohort. Bounded development collection and model screening may proceed with an unqualified reviewer, explicitly pending human audit; default promotion and qualification require the declared human evidence.

Use `scripts/library-audit.ts`, `scripts/library-replay.ts`, `scripts/family-parts.ts`, `src/corpus/load.ts`, `src/pipeline/reconstruct.ts` and the existing20-family development manifest.

- Use the compiler20 dispositions for the1,009source-fidelity differences and438admission refusals. Resolve unknown causes by minimal independent examples and rank by blocked generation families. These populations overlap; never add them as independent defects. Already-supported source fidelity must not regress.
- Prioritize the largest class that blocks useful generation. For mixed paint, support explicit component roles only if source paint and native pixels can be preserved. For CSS/transforms/masks/use, implement the specific needed semantics with independent examples, or keep the source reference-only/refused. Do not turn an unsupported source into an approximate admitted donor.
- After each source-conversion fix, rerun all4,357sources against the same source hash in a new directory; report improved, regressed and unchanged files. Every newly admitted component must pass native fidelity with counters intact. Exact replay alone is insufficient.
- Generate the full20-family development cohort at both native masters:40pair requests/80outputs, reusing earlier results only when compiler/spec/route and manifest identity remain frozen and comparable. Preserve missing/error/rejected rows and stratify by dense details, organic curves, narrow connections, intersecting cutters and asymmetric modifiers.

**Acceptance:** Every source failure is classified with an explicit disposition; zero silently admitted fidelity failures. All80development slots have authentic terminal evidence; visual gates meet target per stratum before opening novel qualification. No source reconstruction counts toward those80outputs.

### 5. Select a route on matched evidence, then freeze it

The bounded screen can collect development evidence after slice2a while labels are pending. Selection and route promotion require human evaluation and the qualified critic from slice3. Retain the currently working native author/reviewer as the baseline; no universal “best model” claim.

Use `scripts/local-generate.ts`, `scripts/local-style-run.ts`, `scripts/local-composition.ts` and the existing experiment receipt format. Verify exact currently callable model IDs, auth and costs before a comparison; historical marketing names are not configurations.

- Start with four difficult development concepts under matched pinned retrieval packets, specs, tooling, meanings and total budgets, at both native masters and both paints:16outputs per author arm. Compare the baseline against one available challenger. Expand to the20family cohort only for a contender that clears all four concepts at both masters without critical failures; a fourconcept screen cannot establish a winner. Provisional model review can eliminate a broken arm but cannot supply missing human acceptance.
- Evaluate retrieval separately from author model quality. Do not change retrieval packets and author simultaneously then attribute any gain to the author. Assess a proposed improved retriever on the same query/source inventory with useful-component recall and final output evidence.
- Add an image-assisted composition arm only for a demonstrated layout failure. Same downstream DSL and review contract; reference/composition images cannot reach blind recognition or be mistaken for generated SVG output.
- Compare quality first, then total latency and cost per accepted pair including rejected/timed-out attempts. Unknown subscription dollar cost stays unknown; report tokens and time separately. A conditional fallback route needs demonstrated benefit for a named failure class and must share the original deadline.

**Acceptance:** A self-contained comparison supports the chosen route or explicitly says inconclusive. Freeze compiler, master specs, retrieval/index/exclusions, prompts, instrument, models, deadline and spend ceiling before qualification. Fine-tuning remains conditional on repeated residual construction-policy failures after adequate geometry/references/feedback; no training spend without verified access, rights, budget and a held-out uplift test.

### 6. Complete catalog coverage and novel-family qualification

Depends on slices1–5. This stage is substantial work, not a final screenshot check.

- Run the frozen route across all4,442catalog concept-size pair requests in resumable, cost-reserved batches. Preserve8,884terminal output rows. Checkpoint by manifest/request/revision hash; never silently reuse an artifact from a different route or charge a resumed request twice. Retries remain attached to the original request denominator.
- Catalog generation may use related donor families and must disclose reconstruction or near-copy results separately. It establishes supported-library behavior, not unseen generalization.
- Before opening new qualification tasks, seal two disjoint batches of20novel semantic families each. Audit source identity, aliases, close visual/geometry duplicates and prior experiment exposure; exclude the entire family from candidates, style anchors, admitted parts and cached retrieval. An exact-name/hash blacklist alone is insufficient. A mountain-bike donor invalidates a sealed bicycle-family claim.
- Generate both paints/native masters for each batch:80outputs per batch/160total. Score each batch independently; per paint-size stratum a20item batch needs at least19passes to reach95%. Report family-cluster uncertainty and avoid stronger population claims than the evidence supports.
- Export complete PNG/SVG/program/audit receipts, all failures and the per-dimension report. Automatic critic approval requires slice3qualification; human craft/family audit remains necessary for the human-rating gate.

**Acceptance:** No missing catalog rows; yield/quality/latency gates pass for the catalog and both novel batches independently; all accepted geometry passes, all human-label requirements satisfied. A failed opened batch becomes regression data, and a newly sealed batch is required after tuning. Only then update the rubric to qualified10/10.

## Verification commands and observations

Run from repository root. Use fresh output directories; placeholder paths below are chosen and recorded in the execution manifest before invoking a command.

- Slice1: `npm exec -w iconsmith -- vitest run scripts/quality-benchmark.test.ts scripts/local-style-run.test.ts scripts/local-retrieval.test.ts` covers pinned-context audit, native-master denominators and shared deadlines. Preserve these completed scenarios when extending the pipeline.
- Slice2: `npm exec -w iconsmith -- vitest run scripts/family-parts.test.ts scripts/style-check.test.ts src/tools/boolean-round.test.ts src/tools/proof.test.ts` plus canonical `npx tsx packages/iconsmith/scripts/local-generate.ts <concept> <new-output> --revision <revision.json> --master <native-master> --meanings <meanings.json> --library <source-svg-directory> --library-set blode-icons`. The shared budget is already implemented; retain it. For slice2a, add shared-packet/per-master admission scenarios to the existing retrieval and family-parts suites, then run the frozen two-arm family manifest through this same canonical entry point.
- Slice3: `npm exec -w iconsmith -- vitest run scripts/local-review.test.ts src/eval/foundry-gate.test.ts scripts/local-style-run.test.ts`; real labeled critic/repair receipts are separate required evidence. A test fixture cannot qualify the instrument.
- Slice4: `npx tsx packages/iconsmith/scripts/library-audit-cli.ts <source-svg-directory> <new-audit-directory>` then `npx tsx packages/iconsmith/scripts/library-replay-cli.ts <inventory.json> <new-replay-directory>`. Compare per-file source-hash-bound results and native images, including every refusal.
- Each material implementation slice, sequentially to avoid dist-clean/build races: root `npm run test`, `npm run typecheck`, `npm run build`, `npm run check`, and `git diff --check`. Current canary1,656tests/132files/0skipped; update legitimate additions in rootAGENTS, investigate skips, and never synthesize the private corpus.
- Final qualification: machine-readable per-stratum numerators/denominators, immutable audit images, labeled craft evidence, critic version, end-to-end timings and reconciled spend. No CLI report can turn missing evidence green.

## Spending, unresolved dependencies and recovery

This plan makes no new provider reservation. Reconcile the live ledger before each batch; the latest recorded state is13.06220785USDconfirmed and11.37126575USDunknown reservations from the earlier round. Native subscriptions are separate, with dollar consumption unknown. Log calls and their actual/unknown cost; no automatic API fallback.

The catalog campaign is not assumed affordable:4,442pair requests at the10minute ceiling represent about740hours of serial wall time before the novel batches. This is an upper-bound workload calculation, not a latency forecast or dollar estimate. Use the frozen development cohort to measure total stage time, retries and usage, then prepare concrete batch manifests, concurrency limits, checkpoint strategy and cost/resource estimates before launching the catalog. Start with a bounded batch and stop further dispatch on budget exhaustion, unresolved charges or a critical regression. Human labeling workload must be included:100sealed critic stimuli, a separate repair set, development comparisons and the full requested human audit population are substantial work. Existing budget authorization is not permission to exceed its cap. Reuse frozen qualifying results by exact manifest identity, not by visual similarity.

Unresolved evidence dependencies: human taste labels and native-master acceptance, qualified critic performance, family/near-duplicate exclusion closure, currently callable challenger models, and measured full-campaign resources. None blocks remaining offline engineering or explicitly unqualified development collection. Human labels cannot be fabricated and elapsed time is not approval.

Preserve every original failed attempt and evidence directory. Each compiler/spec change creates a new revision; each experiment uses a new directory. On a regression, revert only the responsible implementation change and keep its failed receipt. Resume batch work only from validated terminal receipts; retain uncertain charges until reconciled. Do not rewrite shared history or require a compatibility layer to preserve evidence.

## Current execution evidence

- Shared family packets enforce source/exclusion hashes, separate native admission and native reference proofs. The six-family shared arm is complete: 12 terminal requests, 7 review-clear and 5 incomplete. It spans 24 output slots; 22 renderable artifacts and 2 missing outputs are retained. The independent control arm remains 24 pending slots. No matched win or default promotion is established, and implementation snapshots changed between early and later runs.
- [Actual shared-arm images](../log/family-packet-shared-arm-audit-2026-09-07/batch-001.png) include failed artifacts, not only review-clear outputs. Human recognition/craft/family labels remain pending; model reviewers are unqualified.
- [Controlled finalization recovery](../log/native-finalization-recovery-2026-09-07/README.md) exercises actual interrupted native authoring within the original deadline. The selected candidate retains an unresolved counter-display observation; recovery completion does not close shoulder acceptance.
- [Source importer correction](../log/source-line-parser-isolated-fix-2026-09-07.json) improves admission from 3,919 to 3,921 with zero regressions over the exact 4,357-source population. Exact replay remains 4,357/4,357; source raster differences fall from 1,009 to 1,007. The parser fix is now integrated; DSL compiler semantics remain revision20.
- [Source cause probe](../log/source-fidelity-cause-probe-2026-09-07.json) reproduces all 372 prior native-fidelity refusals. Diagnostic finer placement resolves 281 but does not change production grid/admission. [Feature counterfactuals](../log/source-fidelity-feature-causal-probe-2026-09-07.json) isolate selected unsupported paint/viewport causes; all unresolved sources remain explicitly refused/reference-only.
- Qualification tools freeze exact 80-slot development and 8,884-slot catalog manifests and bind human labels to native image hashes. Coverage is never qualification. The sequential development driver validates all 20 families/80 unique slots, binds canonical request/tooling identities, refuses unknown attempts and resumes recovered terminals repeatedly without another provider charge. CLI executable versions are not byte-pinned; a sealed route still needs its external runtime identity frozen.
- Timeout traces exposed repeated reading of the bundled checker. The integrated prompt treats it as an executable, directs repairs from its results and removes duplicate brief rereading. Budgets and inspection/review requirements are unchanged. This prompt correction has regression coverage but no measured provider-run uplift yet.
- Human development labels have been requested and remain pending. Existing exposed development images cannot become sealed critic qualification data.

## Next deliverable

Collect the pending human labels and classify the failed shared-arm outputs. Measure the author prompt correction in a separately declared bounded batch before expanding the 80-output cohort. Finish the native16 shoulder acceptance and the matched family comparison; do not pool these changed snapshots into a route win. Human critic qualification, the independent 20-failure repair set, matched model screen, catalog campaign and two novel batches remain open.

Before new provider calls, record a bounded manifest, resource check, intent and cost reconciliation in the foundry log. The catalog remains gated by frozen-route quality evidence and a measured resource plan.
